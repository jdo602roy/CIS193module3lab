let width = window.prompt("What the width?");
let height = window.prompt("What the height?");
let length = window.prompt("What the length?");

console.log("calculated volume is: ", width*height*length);